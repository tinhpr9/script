-- ====================================================================
-- 🥚 STEAL AN EGG! (SAE) - BAC-4519 BYPASS (100% LEGIT INTERACTION)
-- 🚫 KHÔNG FIREPROXIMITYPROMPT BYPASS - MÔ PHỎNG NGƯỜI CHƠI BẤM NÚT
-- ====================================================================

if not game:IsLoaded() then game.Loaded:Wait() end

local Players = game:GetService("Players")
local Workspace = game:GetService("Workspace")
local VirtualUser = game:GetService("VirtualUser")
local StarterGui = game:GetService("StarterGui")

local LocalPlayer = Players.LocalPlayer
repeat task.wait(0.5) until LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()

-- Các biến toàn cục
_G.AutoFarm = true

-- Tạo thông báo
pcall(function()
    StarterGui:SetCore("SendNotification", {
        Title = "🥚 SAE 100% Legit Mode",
        Text = "Đã nạp Script Bypass BAC-4519 (Mô phỏng bấm nút thật)!",
        Duration = 4,
    })
end)

local function getCharacter()
    local char = LocalPlayer.Character
    if char and char:FindFirstChild("HumanoidRootPart") and char:FindFirstChild("Humanoid") then
        return char, char.HumanoidRootPart, char.Humanoid
    end
    return nil, nil, nil
end

-- Hàm đi bộ hoàn toàn hợp lệ (Legit Walk) để tránh mọi Anti-Cheat (BAC-10511, BAC-8513)
local function walkTo(targetPosition)
    local char, root, humanoid = getCharacter()
    if not humanoid or not root then return false end
    
    humanoid.WalkSpeed = 25 
    humanoid:MoveTo(targetPosition)
    
    local timeout = 6
    local elapsed = 0
    while elapsed < timeout do
        local distance = (root.Position - targetPosition).Magnitude
        if distance <= 4 then
            return true
        end
        task.wait(0.1)
        elapsed = elapsed + 0.1
        
        -- Thỉnh thoảng nhảy nếu bị kẹt
        if humanoid.MoveDirection.Magnitude > 0 and root.Velocity.Magnitude < 1 then
            humanoid.Jump = true
        end
    end
    return false
end

-- Hàm tương tác an toàn 100% (BYPASS BAC-4519)
local function safeTriggerPrompt(prompt, root)
    if not prompt or not prompt.Parent then return end
    
    -- Đảm bảo đã đứng đủ gần trước khi kích hoạt
    local distance = (root.Position - prompt.Parent.Position).Magnitude
    if distance > 10 then return end -- Xa quá không bấm
    
    -- TUYỆT ĐỐI KHÔNG DÙNG fireproximityprompt VÌ SẼ BỊ BAC-4519 PHÁT HIỆN
    -- Mô phỏng chính xác hành động đè nút của người chơi thật
    local holdTime = prompt.HoldDuration or 0
    
    prompt:InputHoldBegin()
    if holdTime > 0 then
        -- Chờ đúng thời gian yêu cầu của game (cộng thêm 0.1s ping mạng)
        task.wait(holdTime + 0.1)
    else
        task.wait(0.1)
    end
    prompt:InputHoldEnd()
end

-- VÒNG LẶP AUTO FARM LEGIT
task.spawn(function()
    print("[🥚] Khởi động vòng lặp Legit Interaction Auto Farm...")
    
    while true do
        task.wait(0.5)
        
        if _G.AutoFarm then
            local char, root, humanoid = getCharacter()
            if root and humanoid then
                local allPrompts = {}
                for _, obj in pairs(Workspace:GetDescendants()) do
                    if obj:IsA("ProximityPrompt") and obj.Enabled then
                        table.insert(allPrompts, obj)
                    end
                end
                
                local acted = false
                
                -- Ưu tiên 1: HATCH (Ấp trứng) hoặc PLACE (Đặt trứng) tại Base
                for _, prompt in ipairs(allPrompts) do
                    local action = prompt.ActionText:lower()
                    if action:find("hatch") or action:find("place") then
                        local targetPart = prompt.Parent:IsA("BasePart") and prompt.Parent or prompt.Parent:FindFirstChildWhichIsA("BasePart")
                        if targetPart then
                            if walkTo(targetPart.Position) then
                                task.wait(0.2)
                                safeTriggerPrompt(prompt, root)
                                print("[✨] Đã tương tác Legit:", action)
                                acted = true
                                break
                            end
                        end
                    end
                end
                
                -- Ưu tiên 2: STEAL (Đi ăn trộm trứng)
                if not acted then
                    for _, prompt in ipairs(allPrompts) do
                        local action = prompt.ActionText:lower()
                        if action:find("steal") then
                            local targetPart = prompt.Parent:IsA("BasePart") and prompt.Parent or prompt.Parent:FindFirstChildWhichIsA("BasePart")
                            if targetPart then
                                if walkTo(targetPart.Position) then
                                    task.wait(0.2)
                                    safeTriggerPrompt(prompt, root)
                                    print("[🥚] Đã trộm trứng Legit (Steal)!")
                                    break
                                end
                            end
                        end
                    end
                end
                
            end
        end
    end
end)

-- Chống AFK
task.spawn(function()
    LocalPlayer.Idled:Connect(function()
        VirtualUser:CaptureController()
        VirtualUser:ClickButton2(Vector2.new(0, 0))
    end)
end)

-- GUI Đơn giản
pcall(function()
    local sg = Instance.new("ScreenGui")
    sg.Name = "SAE_UltraLegit_GUI"
    
    local parentGui = game:GetService("CoreGui")
    pcall(function() sg.Parent = parentGui end)
    if not sg.Parent then
        sg.Parent = LocalPlayer:WaitForChild("PlayerGui")
    end
    
    local btn = Instance.new("TextButton", sg)
    btn.Size = UDim2.new(0, 140, 0, 45)
    btn.Position = UDim2.new(0, 20, 0.5, 0)
    btn.BackgroundColor3 = Color3.fromRGB(0, 180, 80)
    btn.Text = "🥚 AUTO: BẬT"
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 14
    
    local corner = Instance.new("UICorner", btn)
    corner.CornerRadius = UDim.new(0, 10)
    
    btn.MouseButton1Click:Connect(function()
        _G.AutoFarm = not _G.AutoFarm
        btn.Text = _G.AutoFarm and "🥚 AUTO: BẬT" or "🥚 AUTO: TẮT"
        btn.BackgroundColor3 = _G.AutoFarm and Color3.fromRGB(0, 180, 80) or Color3.fromRGB(200, 50, 50)
    end)
end)
