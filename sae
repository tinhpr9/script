-- ====================================================================
-- 🥚 STEAL AN EGG! (SAE) - ADVANCED AUTO FARM & LOADER BY TINHPR9
-- ====================================================================

-- 1. Chờ Game và Nhân vật nạp hoàn tất 100%
if not game:IsLoaded() then
    game.Loaded:Wait()
end
repeat task.wait(0.5) until game.Players.LocalPlayer
local LocalPlayer = game.Players.LocalPlayer
repeat task.wait(0.5) until LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
task.wait(2)

-- 2. Thiết lập FPS & Tối ưu luồng mạng
if setfpscap then
    setfpscap(15) -- Giữ mức 15 FPS an toàn theo chuẩn tác giả (>= 10 FPS)
end

-- 3. Bẻ khóa giả lập PlaceId (Bypass lỗi 'Other Game')
if hookmetamethod and checkcaller then
    local oldIndex
    oldIndex = hookmetamethod(game, "__index", function(self, key)
        if not checkcaller() and self == game and key == "PlaceId" then
            return 16867381283 -- Force Steal An Egg Main Place ID
        end
        return oldIndex(self, key)
    end)
end

-- 4. Bảng Cấu hình người dùng (StealAnEgg Config)
getgenv().StealAnEgg = {
    MaxPlotLevel = 5,
    MaxTreadmillLevel = 10,

    SellPets = {
        { Name = "Centapede", Mutation = "None", MaxKG = 3 },
    },

    PlaceEggs = {
        { Name = "Cosmic Egg", Rarity = "Legendary" },
        { Name = "Cherry Blossom Egg", Rarity = "Cosmic" },
        { Name = "Cherry Blossom Egg", Rarity = "Legendary" },
    },
    PlaceEggSpacing = 4,
    PlaceEggMaxTriesPerEgg = 12,

    StealEggs = {
        { Name = "Cherry Blossom Egg", MinKG = 0, Rarity = "All" },
        { Name = "Cosmic Egg", MinKG = 0, Rarity = "Secret" },
    },

    DiscordId = "123456789012345678",
    PrivateHatchWebhookUrl = "",
    PrivateHatchWebhookRarities = { "Legendary", "Mythic", "Secret", "Eternal", "Divine" },
    ShowPingInGlobalWebhook = false,
}

-- 5. Thông báo hiển thị trạng thái trên màn hình game (StarterGui Notify)
pcall(function()
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = "🥚 Steal An Egg Auto",
        Text = "Đang kích hoạt Auto Farm & Nạp Loader...",
        Duration = 5,
    })
end)

-- 6. Khởi chạy luồng chính an toàn (Protected Execution)
task.spawn(function()
    local success, err = pcall(function()
        loadstring(game:HttpGet("https://api.luarmor.net/files/v4/loaders/f7fb20d3bbdf4ef86ef18caebae5b838.lua"))()
    end)
    
    if not success then
        warn("[SAE Luarmor Error]:", err)
        print("[!] Luarmor nạp không thành công, vui lòng kiểm tra kết nối mạng!")
    end
end)

-- 7. Luồng hỗ trợ giữ kết nối & chống AFK văng game (Anti-AFK Engine)
task.spawn(function()
    local VirtualUser = game:GetService("VirtualUser")
    LocalPlayer.Idled:Connect(function()
        VirtualUser:CaptureController()
        VirtualUser:ClickButton2(Vector2.new(0, 0))
        print("[Anti-AFK] Đã gửi tín hiệu chống văng game!")
    end)
end)
